[project]
name = "web-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.3",
    "pytelegrambotapi>=4.27.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.0",
]
